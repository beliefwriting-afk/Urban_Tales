/**
 * 開發用的位置模擬與佔位回應。
 *
 * ★★★ 切片 7（2026-09-07）之後，這個檔案只剩兩樣東西。★★★
 *
 *   景點、卡片、身分全部改由伺服器給了（`/api/sites`、`/api/collection`、`/api/me`），
 *   所以原本的 `SITES`、`CARDS`、`summonM` 全部刪掉。
 *   ⚠️ 特別是 `summonM`：判定半徑永遠不進 API 回應，前端留著一份等於
 *     自己算得出「我到了」——那正是切片 7 要刪掉的東西（HANDOFF §14.8 第 1 項）。
 *
 * 留下來的兩樣，各有各的理由：
 *
 *   `WALK_PATH`          —— **開發用的位置來源**。桌機沒有 GPS，沒有它就沒辦法
 *                           在電腦上測到場判定。⚠️ 但判定仍然是伺服器做的：
 *                           滑桿的座標會原樣送去 `/api/presence`。
 *                           **假的是位置，不是判準。**
 *   `PLACEHOLDER_REPLIES` —— 對話還沒接（批 2）。⚠️ 每一則都標明自己是佔位，
 *                           理由是 HANDOFF §3 決策 7：靈魂台詞要逐字審，
 *                           不代寫、不預生成，免得哪天被當成真內容抄進 content/。
 */

/**
 * 玩家走的那條路徑（真經緯度）：西門紅樓 → 龍山寺 → 剝皮寮。
 *
 * ★ 路徑節點就是景點座標本身，而且每個景點**重複一次**。
 *   重複的用意是讓滑桿在那個景點上「停一段」：重複的節點之間距離為零，
 *   但 walkAt() 給每一段的滑桿份額是一樣的，所以滑桿會有整整 1/7 的行程
 *   停在那個點上。不重複的話，50 公尺的召喚半徑攤在 900 公尺的路段上
 *   只有不到 1 格滑桿寬，滑過去就錯過了。
 *   （2026-08-25 第一版就是這樣，結果只有西門紅樓進得去。）
 *
 * ⚠️ 這幾個座標是從地圖上讀的近似值。實地量測是 P0-2，六組一組都還沒量過。
 */
export const WALK_PATH: readonly { lat: number; lng: number }[] = [
	{ lat: 25.0455, lng: 121.5069 }, // 起點：西門町北側，還沒靠近任何景點
	{ lat: 25.0421, lng: 121.5069 }, // 西門紅樓
	{ lat: 25.0421, lng: 121.5069 }, // ← 重複一次＝在這裡「停一下」
	{ lat: 25.0395, lng: 121.503 }, // 沿中華路往南（中途點，不是景點）
	{ lat: 25.0372, lng: 121.4997 }, // 龍山寺
	{ lat: 25.0372, lng: 121.4997 },
	{ lat: 25.0359, lng: 121.499 }, // 剝皮寮
	{ lat: 25.0359, lng: 121.499 }
] as const;

/**
 * ⚠️ 佔位回應，不是台詞。批 2 接上 `/api/chat` 之後整個刪掉。
 *
 * 每一則都標明自己是佔位。介面要看的是「氣泡長怎樣、換行怎麼斷、
 * 長訊息會不會撐破版面」，這些用佔位文字一樣看得出來。
 */
export const PLACEHOLDER_REPLIES: readonly string[] = [
	'（介面展示用佔位）這一格會放靈魂的回應。正式台詞由 /api/chat 來，批 2 接上。',
	'（介面展示用佔位）短的一句，用來看氣泡最小寬度。',
	'（介面展示用佔位）這一則刻意寫得長一點，用來確認文字換行、氣泡最大寬度、以及對話窗捲動的行為在手機寬度下是不是還讀得下去。'
] as const;
