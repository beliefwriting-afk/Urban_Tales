# CONTEXT — 城市物語 Urban Tales

> 這份文件是專案的**入口**。任何人（包含未來的我、以及任何 AI 助手）接手時先讀這一份，再依需要展開到其他文件。
> 最後更新：2026-09-03

---

## 一句話

**《城市物語》／ Urban Tales** — 一款手機網頁遊戲：玩家實際走到台北的景點，召喚出該地標的「城市靈魂」，透過對話了解這個地方的故事。

**專案目的**：透過城市探索的遊戲方式，讓玩家了解台北各景點的地方故事。

> 這句話是最高判準。任何設計決策都要能回答：**它有沒有在服務這件事？**
> 玩法好玩但玩家講不出故事 = 失敗；玩家能跟朋友說「原來剝皮寮以前是⋯⋯」= 成功。

---

## 專案基本資料

| 項目 | 內容 |
|---|---|
| 性質 | 個人作品集／技術驗證，不以營收為目標 |
| 人力 | 一人（企劃、程式、美術、內容全包） |
| 期限 | 無硬性截止日，但越快做完越好 |
| 平台 | 手機瀏覽器優先的網頁應用（免安裝） |
| 前端框架 | **SvelteKit 2 ＋ Svelte 5（runes）** |
| 語言 | **TypeScript 全端**。Python 僅用於 `tools/` 的離線工具，不部署 |
| 部署 | GCP Compute Engine 單機（nginx ＋ systemd ＋ 自架 PostgreSQL）＋ Gemini API |
| 內容語言 | 僅繁體中文（架構預留多語言欄位） |
| 美術 | 像素風，**角色除外**（角色為手繪立繪） |
| 專案資料夾 | `C:\Users\erics\Desktop\Urban_Tales` |
| GitHub | `https://github.com/beliefwriting-afk/Urban_Tales`（私有） |

---

## 文件地圖

| 文件 | 是什麼 |
|---|---|
| `CONTEXT.md`（本檔） | 專案入口與現況 |
| `Urban_Tales_企劃書_v0.2.md` | **權威文件**。定義做什麼／為什麼／刻意不做什麼。含決策紀錄（附錄 A）與待確認清單（附錄 B） |
| `Urban_Tales_SDD_v0.1.md` | **系統設計文件**。定義怎麼實作。15 章 ＋ 附錄 A–E；附錄 A 是可推翻的暫定決策表 |
| `HANDOFF.md` | **上一次做到哪、下一步做什麼**。換新對話時，讀完本檔接著讀它 |
| `README.md` | repo 的門面：技術棧、指令、目錄、唯一出口的圖解 |
| `PY_TO_TS.md` | Python → TypeScript 速查表（君和的程式背景是 Python） |
| `City_Soul_AI_對話素材盤點.md` | 前身十個景點的**對話文本與史實**逐字盤點。寫內容時的原料倉庫，**不是內容層**（不進 `content/`、不受 `content:check` 檢查） |
| `ref/anim_compare.html` | Live2D vs 分層 PNG 的動態差異示範，兩邊用同一張圖 |

**前身專案**：桌面的 `City_Soul_AI`（Unity 3D App 版，SDD v2.2）。
本專案**不繼承**其技術棧、分工與後端規劃，只繼承部分玩法邏輯與內容治理原則。
可參考的現成資產：萬華主線腳本〈回家的畫〉、各地標的研究文件、人格卡格式。

---

## 核心設計（十二條）

1. **硬到場** — 必須實際走到景點才能互動。另設**展示模式**（自訂位置），因為作品集的觀眾不會跑到萬華去。
2. **三層介面** — L1 地圖（像素）→ L2 相遇（角色 ＋ 聊天 ＋ 引導提問）→ L3 相機（同介面切換，角色縮右下角可拖曳縮放，快門輸出合成圖）。
3. **兩層內容** — 全景點有基礎層（對話 ＋ 拍照任務）；萬華三站另有劇情層。
4. **角色 = 城市靈魂** — 地標的擬人化集體意識。不是人，是那個地方本身。
5. **無回訪機制** — 沒有共鳴值、沒有每日任務。進度＝成就卡，一趟就能拿完一個景點的卡。
6. **成就卡 13 張** — 相遇卡 5 ＋ 任務卡 5 ＋ 劇情卡 3。卡面是**立繪角色 ＋ 像素卡框**。
7. **景點任務 = 拍一張你覺得最美的照片，無審核** — 定位負責證明在場，照片只是紀念。不上傳、不入卡面。
8. **劇情線性** — 西門紅樓 → 龍山寺 → 剝皮寮（反轉留最後一站）。其餘景點彼此獨立、無順序。
9. **訪客模式為預設** — 零註冊即可玩；綁定 Google 放在設定裡，不做引導提示。
10. **不做真 AR** — iOS Safari 不支援 WebXR immersive-ar，採 0DoF 相機疊層。
11. **AI 不判定進度** — 任務完成與否由確定性規則決定，AI 只負責把結果講得像那個角色會說的話。AI 失效一律回退預寫台詞，且視為正常回應。
12. **材質落差是設計** — 城市是像素的，靈魂不是，因為靈魂不屬於這個世界的材質。要成立必須：共用色盤、交界處以像素框住、角色解析度克制。

---

## 內容治理硬規則

**任何會產出「靈魂說出口的文字」的路徑都必須套用**（對話、引導提問、任務台詞、劇情、保底台詞），**全景點適用，不分景點**。

1. 不扮演真人。歷史人物只能透過靈魂**轉述**存在，不得有直接台詞。
2. 不轉述神明的話語。要解釋靈魂怎麼知道某事，一律用它自己的觀察力。
3. 不對信仰作教義性陳述或裁決：不解籤、不預測吉凶、不比較信仰、不代神明應許。
4. 不是廟方人員、不是宗教解說員、不是導遊。
5. 不對敏感歷史武斷定論，區分**「查得到的事」與「說不清的事」**。
   【2026-09-04 修訂，原本是三分「已知史實／民間傳說／角色想像」】
   前兩者是**素材**，住在 `materials.yaml` 的 `facts`（可以講得斬釘截鐵）與
   `legends`（必須留活口）；「角色想像」不是素材是**權限**，住在 `soul.yaml`
   的 `persona.knows`。三者並列會讓人把想講的話標成「角色想像」塞進素材庫
   ——那等於在素材庫裡開一個不必查證的抽屜。理由見企劃書 §4.2。

> 前身專案的教訓：規則只掛在主要對話路徑上，其餘生成路徑繞過了它。**不要重蹈**。

### ✅ 已經裝上去了（2026-08-25）

這條規則不再只是文件上的要求，**已經在程式碼裡機械化**：

```
src/lib/server/ai/client.ts     ← 唯一可以 import 'openai' 的檔案
        ▲  只有這一條線
src/lib/server/soul/speak.ts    ← 唯一可以 import AI client 的檔案
        ▲
    其他所有程式碼               ← ESLint 直接擋，CI 建置失敗
```

- `eslint.config.js` 的 `FENCE_DEFAULT` / `FENCE_EXEMPT_*` 三個區塊負責封鎖
- `npm run test:guard` **驗證這道圍籬本身沒有失效**（六個案例：四個該擋、兩個該放行）
- 全域護欄文字集中在 `content/guardrails.yaml`，**全站唯一一份**，任何景點不得覆寫
- `npm run content:check` 有一條靜態檢查會擋掉 persona 裡的「神明說」類字串

> ⚠️ 實作過程中 `test:guard` 抓到一個真的漏洞：ESLint flat config 裡同一條規則後面的設定會**整個覆蓋**前面的，導致兩道圍籬互相蓋掉、`openai` 被放行。已修，並在設定檔留了註解。**這證明了 SDD §14「Lint 規則測試」那一條的必要性**——沒有它，漏洞會安靜地存在。

---

## 隱私硬約束

- 不做背景定位追蹤，僅在玩家主動使用時前景取得位置
- 座標用於判定後即丟棄，**不建立任何位置軌跡類的資料**
- 不上傳、不保存玩家照片
- 訪客模式不蒐集個人資料

**已在 schema 層落實**：`src/lib/server/db/schema.ts` 沒有任何經緯度欄位，且 `content:check` 有一條檢查會掃 `lat`／`lng`／`geography`／`geometry` 欄位定義，出現即建置失敗。

---

## 目前狀態

**階段**：✅ 骨架、P0-4、介面展示、地圖圖層、**後端四支端點**都完成。
✅ **已上線：https://urbantales.alcloud.us** ⚠️ P0-5 要重做（換了 AI 供應商）。

**細節與下一步請看 `HANDOFF.md`。** 摘要：

1. ✅ SDD v0.1 完成（15 章 ＋ 附錄 A–E）
2. ✅ 前端框架定案 SvelteKit；並在 2026-08-25 重新評估過改用 Python 的可能，結論是維持（理由見 HANDOFF §2）
3. ✅ 排除了擋路的系統問題（OneDrive 已知資料夾重導向）
4. ✅ Windows 工具鏈確認並補齊：Node 24.19.0（fnm）、npm 11.17.0、git 2.55.0
5. ✅ 專案骨架產出，**含唯一出口的 ESLint 規則，第一天就裝上了**
6. ✅ `git init` → `npm install` → 私有 GitHub repo → 首次 push 完成（commit `86e9741`，49 檔）
7. ✅ 驗收全過：`content:check` / `lint` / `test`（8 個）/ `test:guard`（6 個）/ `check`（0 錯）/ `build`
8. ✅ **P0-4 完成**（2026-08-25）：Cubism FREE 版**可以**匯出 `.moc3`，Live2D 路線**沒有授權費**。
   真正的約束是動作參數額度——範本已佔 27／30，只剩 3 個。詳見 SDD §9.4
9. ⚠️ **P0-5 要重做**：2026-08-25 對 Zeabur AI Hub 量的（延遲 0.79～1.28 秒、不回報 `cached_tokens`），
   **已隨 2026-09-03 搬到 Gemini 直連而作廢**。成本仍按無快取估算（保守，1,000 名玩家全破約 $116）。
   仍然成立的一項：護欄實際佔 1,139 tokens，是原估的 2.75 倍。詳見 SDD §6.3
10. ✅ **介面展示完成**（2026-08-28）：三層介面在手機瀏覽器可完整操作，全部餵假資料。
    像素地圖、Cubic 11 點陣字型、九宮格像素邊框、三個浮空視窗。**詳見 HANDOFF §10**
11. ✅ **地圖圖層完成**（2026-08-28）：OpenStreetMap 資料 → Python 離線光柵化 → 像素圖磚。
    範圍是**整個台北市**（21.5 × 28.3 km），630 張磚共 3.61 MB，手機一次載視野內約 9 張 ≈ 53 KB。
    前端可平移、五階縮放、方位旋轉、羅盤跟隨；展示模式下長按地圖可自訂位置。**詳見 HANDOFF §12**
12. ✅ **後端開工**（2026-08-29～09-03）：訪客身分、`GET /api/sites`、`POST /api/presence`
    ＋ 展示模式三支端點完成，51 條單元測試 ＋ `npm run smoke:api` 端到端檢查。
    三站內容（人格卡、素材庫）從前身專案轉過來了。**詳見 HANDOFF §13**
13. ✅ **搬到 GCP 並上線**（2026-09-03，資安考量）：**https://urbantales.alcloud.us**
    GCP 專案 `larp-507213`／e2-small／自架 PostgreSQL（只綁 loopback）／Gemini 直連。
    21 條煙霧測試對正式站全綠。踩到的三個坑見 HANDOFF §13.9
14. ✅ **切片 4 完成 ＋ 開發環境修好**（2026-09-03）：`POST /api/site/:id/enter`
    ——草稿站拒絕、首次進入發相遇卡（冪等）。`npm test` 現在 75 條。
    本機 `.env` 改名 `AI_*` 並改走 SSH 通道連 VM 上的 Postgres。
    ★ 同時發現 **`npm run verify` 的判準跟 CI 對不上**，讓一個型別錯誤躺了六天；
    `verify` 已補上 `npm run check`。**詳見 HANDOFF §14**
15. ✅ **內容規劃改版**（2026-09-04）：素材庫從「13 條 items ＋ kind／topic」改成
    **一段 `facts` ＋ 幾條 `legends`**；引導提問從「≥12 題 ＋ tier 挑選」改成
    **恰好 3 題、寫死**。schema、content:check、範本、三站素材、SDD、企劃書
    全部同步改完。**詳見 HANDOFF §15**
16. ⬜ 之後：切片 5–7（chat／快門圖鑑／前端接上）、P0-1（角色動畫）、
    P0-2（實地量測，**已解鎖**）、P0-3（相機合成）、地圖色調

**最大風險**：單人的美術產能（5 隻角色 ＋ 13 張卡 ＋ 一整套像素資產）。
對策：P0 先完整做一隻並計時，再決定其餘四隻要不要降規。
SDD §9.4 的 `SoulRenderer` 抽象層已讓「Live2D vs 分層 PNG」這個未定案**不再阻擋開工**，介面已經寫進 `src/lib/client/soul/renderer.ts`。

**景點範圍已拍板（2026-08-28）**：**只做萬華三站**——西門紅樓 → 龍山寺 → 剝皮寮。
其餘景點延後。連帶解鎖了 P0-2 實地量測（以前卡在不知道要去哪量）。
⚠️ 三站之下展示層的成就卡是 **9 張**，不是 13 張。13 張仍是五站齊備時的目標值，
定義在 `content/`（目前 0 站），`content:check` 的 #7 規則也還沒啟用——**規格沒有改**。

**尚未定案的重要項目**：見企劃書附錄 B 與 SDD 附錄 A。地圖方案（附錄 A #2 / T9）已拍板結案。

---

## 已推翻／不要重做的評估

- **共鳴值系統**（每個靈魂各自累積、多階段解鎖）——與硬到場相衝，會要求玩家為同一景點跑五趟。已改為成就卡。
- **當日情境／回訪內容管線**——前身專案最大的坑（表建好了但沒人生產內容，導致每天都一樣）。本專案不押在回訪上，直接繞過。
- **真 AR（WebXR）**——iOS Safari 不支援，跨平台只剩付費 SDK。
- **拍照辨識判定**——一旦拿掉，辨識成本、失敗率、實勘出題、景點整修導致題目失效等問題全部消失。
- **專案搬離桌面到 `dev\`**——原本是為了躲 OneDrive 同步，OneDrive 已徹底移除，不需要了。
- **改用 Python（前端或前後端分離）**——2026-08-25 完整評估過，結論是維持 TypeScript 全端。**理由見 HANDOFF §2，不要重做這個評估。**
- **地圖用單張 PNG**（SDD 原本的 T9）——範圍改成整個台北市之後不可行：解碼後要 152 MB 貼圖，iOS Safari 會砍分頁。已改為圖磚，見 HANDOFF §12。
- **地圖用即時向量渲染**（MapLibre 等）——2026-08-28 評估後否決：像素風與向量渲染體質衝突（天生反鋸齒），且共用色盤做不到，還多一個執行期外部依賴。
