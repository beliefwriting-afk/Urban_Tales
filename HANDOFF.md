# HANDOFF — 接手指南

> **這份描述現況：專案走到哪、下一步做什麼、哪些坑不要再踩。**
> 新對話開始時，讀完 `CONTEXT.md` 之後接著讀這一份。專案定義看企劃書與 SDD。
>
> ⚠️ **章節編號不重用，子章節也是。** 已刪除的：§7、§8、§11（結案或被取代）、
> §13.6、§14.6、§15.4、§16.6（內容已經不成立或被取代）。編號留空——重用編號會讓「§13 是什麼」
> 在不同文件裡有兩個答案，跟 `content:check` 的規則編號同一個道理。

---

## 0. 現況與下一步

### 專案走到哪

| 層 | 狀態 |
|---|---|
| 環境與骨架 | ✅ 完成，唯一出口的 ESLint 護欄第一天就裝上並有測試（§4） |
| 介面 | ✅ 三層介面在手機瀏覽器可完整操作，**餵的還是假資料**（§10） |
| 地圖 | ✅ OSM → 離線光柵化 → 630 張像素圖磚，涵蓋整個台北市（§12）。**色調還沒調** |
| 後端 | ✅ 訪客身分、`GET /api/sites`、`POST /api/presence`、`POST /api/site/:id/enter` ＋ 展示模式三支（§13、§14） |
| 部署 | ✅ **已上線：https://urbantales.alcloud.us**（GCP 單機，§13.7–13.9） |
| 內容 | ✅ 六站各五份 YAML 全部就位，**全部 `status: draft`**（§16）。剩立繪與相遇卡 |
| 角色美術 | ⬜ 一張立繪都還沒有（P0-1） |

`npm test` 75 條、`npm run verify` 全綠。

### 下一步（照這個順序）

1. **切片 5** `POST /api/chat` ＋ `speak()` 九步 —— 最大的一塊，內容前提已經備齊。
   ⚠️ **動手前要先決定一件事**：`speak.ts` 的 `FallbackReason` 有七個值，
   `FallbackSchema` 只有五組台詞，其中 `rate_limit`、`input_too_long`、`output_rejected`
   **沒有指定要回哪一組**（SDD §6.5 的階梯也沒寫）。若 `input_too_long` 落到
   `aiUnavailable`，玩家會收到「我沒聽清，再說一次」，於是把同一段超長輸入再送一次
   ——**確定性死迴圈**。要嘛補明對應，要嘛加一組 `inputTooLong`。
2. **切片 6** 快門回報 ＋ 圖鑑（`GET /api/collection`，`cards.ts` 的 `listCards()` 已備好）。
3. **切片 7** 前端 `mock/session.svelte.ts` 換成真 API。
   ⚠️ **不只是換資料來源**，有**四個**落差，見 §14.8。其中兩個是同一種病——
   **前端有能力自己宣告「我到了」與「我是展示模式」**，那兩段程式碼必須消失，
   不是改成讀 API。第 2 項還有安全性後果（密語現在寫在前端）。
4. **P0-5 重做** —— 換 Gemini 之後延遲與成本都要重量，**連 thinking level 一起量**（§13.7）。
5. **P0-1** 角色動畫（**6 隻**）、**P0-2** 實地量測（**6 組座標**，大同區三組完全沒量過）。
6. **P0-3** 相機 ＋ 角色合成、**地圖色調**（§12.6）。

### 第一站要轉 `playable` 時會撞到

1. `content:check #7b` —— `cards.yaml` 沒有那一站的相遇卡（**這條比 #7 早撞到**）
2. `content:check #8` —— `soul.yaml` 的 `art` 還是 null（等 P0-1）

★ **這兩條 `content:check` 每次都會逐站唸出來**，不必自己記——五份檔案齊了之後，
它會改印「改成 playable 之前還要：⋯⋯」並列出缺的東西。
（**五份檔案齊了不等於可以改 status**，那行訊息原本會讓人以為可以。）

3. 兩關都過之後，`smoke:api` 的「進入景點」才測得到成功路徑——**記得回去把那幾條補上**
4. 六站**全部** playable 之後，`content:check #7` 才開始強制卡片總數 15（6/6/3）

### 有真玩家之前一定要做的

- **資料庫的邏輯備份。** GCP 有每日磁碟快照，但那是 crash-consistent、只能整台還原、
  看不到內容；`pg_dump` 才能單表還原、才讀得懂。
- **展示模式的 UI 標示。** SDD §5.4 要求全程顯示標示條、成就卡標註「展示模式取得」，
  現在只有伺服器端判定，前端還沒做。
- **90 天換 Google 帳號的 SOP。** `Desktop\Roku_AI` 與 `Desktop\LARP_AI` 各有一份。
- **`DEMO_PASSPHRASE` 換成隨機字串。** 現況 `wanhua` 是跟專案主題直接相關的字典詞，
  而 `/demo` 沒有速率限制。炸開範圍有限（每日 40 則，上面還有 L4 壓著），但改起來只要
  改一個環境變數。
- ⚠️ **另開 `urban_tales_dev` 資料庫。** 現在本機開發直接連正式庫，
  而且 `smoke:api -- --purge` 會清 `players` 表——有真玩家之後絕對不能再跑。

### 已知的小事

`MapLayer.svelte:338` 的地圖 div 有 pointer 事件但沒有 ARIA role，
`npm run check` 每次都會唸。補個 `role="application"` 就好，
還沒動是因為它會改到 UI，君和想跟實機驗收一起做。

---

## 1. 專案基本資訊與環境

| 項目 | 內容 |
|---|---|
| 專案資料夾 | `C:\Users\erics\Desktop\Urban_Tales` |
| GitHub | `https://github.com/beliefwriting-afk/Urban_Tales`（私有） |
| 開發環境 | Windows 11 家用版、VS Code、PowerShell 7.6.5（**無**系統管理員權限） |
| Node | **24.19.0**，由 **fnm** 管理（`%LOCALAPPDATA%\Programs\fnm`） |
| npm | 11.17.0 |
| git | 2.55.0，`user.name` / `user.email` / `init.defaultBranch=main` 都已設定 |
| 部署 | **GCP Compute Engine 單機**（2026-09-03 由 Zeabur 遷出，見 §13） |
| AI | **Gemini**（OpenAI 相容端點，AI Studio 免費層） |
| 資料庫 | **VM 上自架 PostgreSQL**，只綁 127.0.0.1，開發機走 SSH 通道 |
| 網域 | `urbantales.alcloud.us`（Cloudflare，DNS only 灰色雲朵） |

### 文件閱讀順序

1. `CONTEXT.md` — 專案入口
2. `Urban_Tales_企劃書_v0.2.md` — **權威文件**，定義做什麼／為什麼／刻意不做什麼
3. `Urban_Tales_SDD_v0.1.md` — 定義怎麼實作。**附錄 A 是可推翻的暫定決策表，每一項都附連動影響**
4. `README.md` — repo 門面，指令與目錄速查

寫內容時另外兩份：`對話內容規劃_v0.1.md`（六站的角色設計與驗收基準）、
`City_Soul_AI_對話素材盤點.md`（前身的原料倉庫，**史實表有五個硬傷，見 §16.2**）。
兩份都**不是內容層**，不進 `content/`、不受 `content:check` 檢查。

---

## 2. ⚠️ 已完整評估並否決：改用 Python（不要重做）

君和的程式背景是**只會 Python**，因此問過「可以全部改用 Python 嗎」。
這件事被完整評估過，結論是**維持 TypeScript 全端**。理由留在這裡，避免下次重跑一遍：

**前端不可行**（不是不建議，是物理限制）：

- 瀏覽器只執行 JavaScript
- Pyodide / PyScript：Python runtime 本身數 MB、啟動秒級。玩家是**站在街邊用行動網路**開這個網頁的，直接撞到核心設計第 1 條「硬到場」
- Reflex / NiceGUI / Flet：本專案三個最難的技術點（`getUserMedia` 相機即時疊層、Canvas 像素地圖、立繪拖曳縮放）全是瀏覽器原生 API 的精細操作，這類框架碰到就要寫自訂元件 —— 而自訂元件就是 JavaScript，只是多隔一層抽象要 debug

**前後端分離（FastAPI ＋ SvelteKit）也否決**：

- 本專案後端極薄（定位判定、AI 轉發、成就卡讀寫、內容驗證，數百行量級），主體難度全在前端
- 分離的好處只作用在薄的那一半，代價（兩個部署單元、兩套環境變數、型別斷開要人工對齊）整個專案在付 —— **槓桿是反的**
- 「團隊分工」「多客戶端」「獨立擴展」「故障隔離」對一人專案 ＋ 純網頁 ＋ 已設計降級 的本專案皆不成立
- 而且對只會 Python 的人來說它是**最差**選項：前端該學的一件都沒少，只是額外多背了部署複雜度

**Python 的正當位置**：`tools/` 底下的離線工具，不部署、不進 runtime、不影響 CI 判定。
判準寫在 `tools/README.md`：**這個腳本的產出會不會被 CI 拿來決定建置過不過？會 → TypeScript 放 `scripts/`；不會 → 放 `tools/`，語言隨意。**

> 特別注意：`content:check` **不能**改用 Python 重寫，因為它跟執行期共用 `content/schema.ts` 的 Zod 定義。
> 另寫一份 pydantic 會造成兩份會漂移的 schema —— 那正是企劃書 §4.2 記錄的前身專案失敗模式換個位置復發。

---

## 3. 仍然有效的定案

| # | 決策 | 理由 |
|---|---|---|
| 1 | **維持 TypeScript 全端** | 見上面 §2 |
| 2 | **Node 用 fnm 管理，不用 nvm-windows** | nvm-windows 靠 symlink 切版本，需要提權或開發者模式，君和過不了。fnm 直接改 shell 的 PATH，零提權，且原生讀 `.nvmrc` |
| 3 | **`.gitattributes` 走 LF，不改全域 `core.autocrlf`** | 他全域是 `autocrlf=true`，其他十個專案吃這個設定，改了會造成假 diff。`.gitattributes` 的 `eol` 優先權高於 `core.autocrlf`，只影響本 repo，而且跟著 repo 走，換機器行為一致 |
| 4 | **`*.md` 排除在 Prettier 之外** | 企劃書、SDD 有大量對齊表格與 ASCII 架構圖，Prettier 會重排。文件排版是內容的一部分，由人負責 |
| 5 | **卡片總數檢查「自動啟用」** | 草稿站沒有卡片，所以 #7 用**可遊玩站數**當判準（不是資料夾數），六站全部 playable 才開始強制。未啟用時會**明確印出來**，不靜默跳過 |
| 6 | **migration 要有真的 DB 連線才生得出來** | `npm run db:generate`。migration `0000` 已含七張表（見 §14.7） |
| 7 | **靈魂台詞要逐字審** | 措辭即成品（企劃書 §4.3）。`content/_template/` 有五個註解齊全的範本可複製 |

---

## 4. 骨架裡有什麼

```
content/            內容層（進 Git，不做 CMS）
  schema.ts           ★ Zod 定義，型別與驗證的單一事實來源（SDD §2.3 全量）
  guardrails.yaml     ★ 全站唯一一份安全界線，8 條
  cards.yaml          空的，等景點拍板
  sites/<id>/         六站，各四份 YAML（缺 fallbacks.yaml）
  _template/          新增景點時複製這裡（五個 YAML，註解齊全）
scripts/            建置期工具（TypeScript，影響 CI 判定）
  content-check.ts    SDD §2.4 十項 ＋ 隱私 schema 掃描
  guard-test.ts       ★ 驗證護欄本身有效
tools/              離線工具（Python，不部署）
src/lib/server/
  soul/speak.ts       ★ 唯一出口（型別完整，九步驟以 TODO 註解列出）
  ai/client.ts        ★ 唯一 AI SDK 入口（含本機 mock 模式）
  presence/geo.ts     haversine ＋ 到場判定（純函式，8 個測試全過）
  db/schema.ts        七張表（SDD §3.2 全量），★ 無任何經緯度欄位
src/lib/client/
  soul/renderer.ts    SoulRenderer 介面 ＋ 工廠（SDD §9.4）
```

**驗收結果（在君和的機器上跑的，不是沙箱）**：

| 指令 | 結果 |
|---|---|
| `npm run content:check` | ✅ 護欄 8 條，卡片檢查正確顯示「尚未啟用（0/5 站）」 |
| `npm run lint` | ✅ 0 問題 |
| `npm run test` | ✅ 通過（當時 8 條，現在 75 條） |
| `npm run test:guard` | ✅ 6 個案例全對 |
| `npm run check` | ✅ 0 錯誤 |
| `npm run build` | ✅ |
| 實測違規 `import OpenAI from 'openai'` | ✅ **ESLint 擋下並印出中文說明** |

> **`npm run verify` 是提交前該跑的那一個指令**
> （= content:check ＋ lint ＋ **check** ＋ test ＋ test:guard）。
>
> ★ `check` 是 2026-09-03 補進去的，理由見 §14.5：**verify 的判準必須與 CI 一致**，
>   否則 CI 會擋、verify 不會擋的那些項目會安靜地爛掉。

---

## 5. 環境與工具鏈踩過的坑

### 5.1 ⚠️ 給君和的 PowerShell 指令必須「貼上安全」

PowerShell 主控台在貼上多行時，看到 `}` 單獨在行首且語句已完整，會**當場送出執行**，導致下一行的 `else` 變成孤兒語句而報錯（`Unexpected token 'else'`）。

規則：

- `if` / `else` 壓在**同一行**，或改用行內的 `$(if (...) { ... } else { ... })`
- **絕不讓 `else` / `elseif` 出現在行首**
- 雙引號字串裡的子運算式只用單引號，避免巢狀雙引號
- `foreach (...) {` 這種以開括號結尾的多行區塊是安全的（parser 仍在續行狀態）
- 長腳本結尾加一行 `Write-Host "(列表結束)"` —— 他貼回來的輸出**經常在最後一個指令被截斷**
- 破壞性操作一律加**安全閘門**（先檢查前提條件，不符就不執行），並印出檢查結果

### 5.2 ESLint flat config 的規則會「整個覆蓋」，不是合併

原本把兩道圍籬寫成兩個設定區塊（一個擋 `openai`、一個擋 `ai/client`），
結果後者把前者**整個蓋掉**，`openai` 被放行 —— 護欄安靜地失效了。

正解：一條規則一次列完所有限制，再用「豁免區塊」對特定檔案重新宣告剩下的限制。
`eslint.config.js` 裡有註解說明，**別重構回兩個區塊的寫法**。

> 這個洞是 `npm run test:guard` 抓到的。**這就是 SDD §14 那條「最容易被忽略」的測試存在的理由**——
> 沒有它，「沒有錯誤」跟「規則有效」長得一模一樣。

### 5.3 ⚠️ 開發機的 IPv6 路徑壞了（不是程式的錯）

打 `hnd1.aihub.zeabur.ai` 時約四成請求以 `WinError 10054`（連線被重置）失敗，
而且都在 **0.04～0.07 秒**內失敗——快到連 TLS 握手都來不及。

分層診斷（`tools/p0_5_conn_diag.py`，不需金鑰）定位到：

| 層 | 失敗率 |
|---|---|
| TCP 三向交握（小封包） | **0%** |
| TLS 握手（憑證鏈，大封包要分片） | **45%** |
| HTTPS 請求 | 50% |

**小封包全過、大封包一半掛掉 = PMTU 黑洞的典型形狀**，而它幾乎總是發生在 IPv6
（IPv6 沒有路由器分片，路徑上任一段 MTU 不足就直接丟，ICMP 又常被擋，發送端
永遠不知道要縮小封包）。curl 強制協定驗證定案：

```
curl -4  →  0 / 12 失敗
curl -6  →  3 / 12 失敗
混合     →  7 / 15 失敗
```

**這是本機線路問題，不是 AI Hub 的問題，Zeabur 上的容器不會遇到。**
所以**不要**為它去改 SDD §6.5 的重試策略——那是為了本機環境去改產品架構。

對策：

- Python 工具在程式內強制 IPv4（`p0_5_aihub_ping.py` 的 `FORCE_IPV4`，
  做法是濾掉 `socket.getaddrinfo` 的 AF_INET6 結果）
- Windows 調整 IPv6 優先權要改 `HKLM` 登錄檔，**需要管理員權限，過不了**，別浪費時間
- Node 20+ 預設開啟 Happy Eyeballs（`autoSelectFamily`），IPv6 失敗會自動退回 IPv4，
  所以 `client.ts` 走 `openai` SDK 時**可能根本不會遇到**。待 P0-1 順手驗證
- **看到 10054 不要以為是程式壞了**——先跑 `p0_5_conn_diag.py` 確認是不是這條線路

### 5.4 Cubism 的限制不是「功能鎖住」，是「規格上限」

社群上大量「FREE 版匯不出 moc3」的災情，追下去幾乎都是**模型超過額度**，
或是**在動畫檔（`.can3`）上找匯出**——不是功能被鎖。

因此**不要拿官方 sample model 驗 FREE 版**：Hiyori、Mao 那些都是 PRO 規格做的，
ArtMesh 動輒兩三百個，在 FREE 版一定失敗，而失敗原因會把人導向完全錯誤的結論。

另外兩個順手撿到的：

- 單張 PNG 不是 Cubism 的標準匯入來源（標準是 PSD）。要先 `File → New` 建好模型工程，
  再拖圖進去，否則會跳 `Modeling View must be active to use.`
- 匯入圖片時對話框問 `Texture` 還是 `Guide image`——要選 **Texture**。
  Guide image 只是描圖參考底圖，不會變成模型的一部分

### 5.5 OneDrive 已知資料夾重導向（已修好，別重做）

Desktop / Documents / Pictures 都已指回 `C:\Users\erics\`，OneDrive 資料夾與環境變數皆已清除，
本次已驗證（`Test-Path $env:USERPROFILE\OneDrive` = False）。

過程中的教訓：Pictures 用圖形介面會失敗，因為底下有巢狀的已知資料夾；
手動路線是 `robocopy /E /MOVE` ＋ 改 `User Shell Folders` 與 `Shell Folders` 兩處登錄檔，
**寫回時務必保留原值型別**（REG_EXPAND_SZ vs REG_SZ）。

---


## 6. P0 技術驗證

### ✅ P0-4 — Cubism FREE 匯出 `.moc3`（2026-08-25 完成）

**結論：FREE 版可以匯出，Live2D 路線沒有授權費。** 以 Editor 5.3.03 FREE 版實測，
完整走完「匯入 → 紋理圖集 → 匯出」。FREE 版商用授權涵蓋「一般使用者與年營收低於一千萬日圓者」，
本專案在範圍內。完整記錄見 SDD §9.4。

**成本沒有消失，是換到額度上**：

| 額度 | FREE 上限 | 新模型範本已佔用 | 餘額 |
|---|---|---|---|
| 材質圖集 | 1 張／最大 2048px | — | 1 張 |
| Part | 30 | 16 | 14 |
| **動作參數** | **30** | **27** | **3** ⚠️ |

- 材質尺寸選 4096px 時 Editor 的 OK 鈕會變灰——**這條軟體會擋，不必人記**
- **動作參數只剩 3 個，而且沒有任何守門**。畫第一隻角色之前要先清掉範本裡用不到的
  預設參數（手臂、身體 Z 軸、細分眉毛）。本專案要的表演（呼吸、眨眼、視線跟隨、
  嘴型、微幅擺動）都在預設集內，夠用但**沒有揮霍空間**
- 已產出 **SDK 5.3（版本位元組 6）與 4.2（版本位元組 4）兩份 moc3** 備用，
  放在 `tools/out/p0-4/`。⚠️ **該路徑在 `.gitignore` 內，不會進 repo**，P0-1 要用時記得複製出來
- **角色資產檔名一律 kebab-case**——Editor 匯出預設帶空格（`Untitled Model.moc3`），
  走 HTTP 路徑要 URL encode，載入失敗時極難查

### ⬜ P0-5 — AI 供應商連通、延遲與成本（要重量）

**現況：端點通、程式寫好了，但量測數字要重做。** 2026-08-25 量的是 Zeabur AI Hub，
2026-09-03 搬到 Gemini 直連之後那些數字不適用。金鑰在 aistudio.google.com 產生，
填進 `AI_API_KEY`。

**仍然成立、不必重驗的兩項：**

| 項目 | 值 |
|---|---|
| 護欄實際成本 | `content/guardrails.yaml` = **1,139 tokens**，是原估 400 的 **2.75 倍** |
| 成本模型 | 一律採**無快取**估算：每輪 $0.00077，1,000 名玩家全破 **$116**。固定前綴 5,100 tokens |

★ 無快取估算**不依賴任何樂觀假設**，這是它最重要的性質。
`client.ts` 的 `cachedTokens` 欄位保留，但**不可拿來推算成本**。

**重量的時候要注意三件事：**

1. ⚠️ **每次要換 user 訊息。** 閘道層有回應快取，完全相同的請求會在約 0.17 秒回同一份
   答案——那不是推論速度，會讓數字好看得不真實。第一版腳本就栽在這裡。
2. ⚠️ **連 thinking level 一起量。** 這是搬家時最重要的發現，見 §13.7——
   `medium` 思考的延遲中位數就是 8.05 秒，而 SDD §6.5 的逾時是 8 秒。
   拿它當主要模型，一半以上的對話會走保底台詞。
3. 工具是 `tools/p0_5_aihub_ping.py`（零依賴、強制 IPv4、不進 CI）。強制 IPv4 的原因見 §5.3。

> `.env` 的 `AI_API_KEY` 留空 = **本機 mock 模式**，`complete()` 直接回固定字串
> 不燒額度。這是刻意的（SDD §13.2）。⚠️ 反過來說，正式環境忘了填，
> 整個對話系統會安靜地變成假的。

### ⬜ P0-1 / P0-2 / P0-3

- **P0-1** 角色在手機網頁上會動（`LayeredPngRenderer` 先做，記錄實際工時）。
  Live2D 那版可直接拿 `tools/out/p0-4/` 的兩份 moc3 試載，**先確認 `pixi-live2d-display`
  搭配的 Cubism Core 吃得下哪一版**，再決定角色正式匯出用 5.3 還是 4.2。
  ⚠️ 那個路徑在 `.gitignore` 內，不會進 repo，要用時記得複製出來。**現在是 6 隻角色。**
- **P0-2** 定位判定實地量測。`resolvePresence` 已經寫好且有測試，缺的是真實座標與
  `radiusM`。**現在是 6 組座標**，大同區三組完全沒量過。
  ⚠️ 剝皮寮是長條形街區，很可能需要 `extraCenters`；霞海城隍廟廟身只有四十幾坪，
  半徑不能照抄別站。
- **P0-3** 相機 ＋ 角色合成輸出（iOS ＋ Android 各一台）。

---

## 9. 與君和協作的方式

- 稱呼「君和」，用「你」不用「妳」
- **辦正事前先講方案、取得明確確認才動手**（他的偏好設定明訂）
- 技術選擇要附邏輯根據，不要只給結論
- 破壞性操作前先診斷、先驗證、先講風險，不要用猜的動他的系統
- **git 指令他要自己在 VS Code 終端機下**，不要用工具替他跑。AI 的角色是給逐條指令、說明每一步做什麼、在旁邊看輸出排錯

### ★ 他的程式背景是「只會 Python」

寫程式碼時：

- 附 **Python 對照註解**——「這行等於 Python 的什麼」，不要寫空泛的 `// 設定路由`
- 型別先幫他寫好，讓他踩在上面填函式內容，靠編輯器自動補完學
- **主動指出坑，不要等他自己撞**。已寫在 `PY_TO_TS.md`：
  空陣列是 truthy、`null` vs `undefined`、`.sort()` 預設按字串排、
  負索引不能用、標準庫幾乎沒有
- 好用的對照：Zod ≈ pydantic、Drizzle ≈ SQLAlchemy、Vitest ≈ pytest、
  tsc ≈ mypy、npm ≈ pip、`package.json` ≈ `pyproject.toml`、`node_modules` ≈ `.venv`
- 真正花時間的**不是 TypeScript，是 Canvas 與瀏覽器 API**——那是專案本身的難度
- P0-1 要拆得比原計畫更細，先給他一個「動起來了」的小成功，再進角色動畫

---

## 10. 介面層

### 10.1 介面層有什麼

在既有的 SvelteKit 骨架裡做出**可在手機瀏覽器完整操作**的三層介面。
commit：`d138f52`（骨架）→ `d3b7a48`（像素化）→ `b59dfad`（三個視窗）。

```
src/lib/styles/tokens.css       色票、尺寸、像素框、字型微調——全站唯一一份
src/lib/client/mock/            ★ 之後接後端時，要換掉的只有這一層
  data.ts                         假景點、走路路徑、假卡片、佔位回應（★ 還停在萬華三站）
  session.svelte.ts               三層狀態機、距離判定、對話歷史、展示模式
src/lib/client/ui/              ★ 接後端時這一層一行都不用改
  terrain.ts   程序生成的像素地形（下一步要換掉的就是它）
  MapLayer  SoulLayer  TopBar  ChatLayer  InputRow
  FloatWindow  WinChat  WinCards  WinSettings  Toast  DebugBar
tools/gen_px_frame.py           像素邊框產生器
static/fonts/cubic-11.woff2     Cubic 11 點陣字型（391KB，含 OFL 授權文字）
```

**架構上最重要的一件事**：UI 元件只跟 `mock/` 講話，不直接碰資料來源。
接後端時換掉 `mock/` 的內部實作即可，元件不動。這是「先確認介面、再做後端」
能真的省到工的前提——做成「原型」與「正式版」兩份會產生同步負擔，
那正是企劃書 §4.2 記錄的前身失敗模式。**所以路由做在 `/`，不是 `/preview`。**

### 10.2 從前身專案繼承的 vs 推翻的

介面沿用 `City_Soul_AI/citysoul-client` 的 `MapScreen.uxml`／`.uss`。
那份 UXML 的註解裡記著大量**實機驗證過的決策**，跨技術棧仍然成立，已照抄：

- 麥克風在輸入框**內側靠右**（放左邊單手持機拇指構不到）
- 快門在畫面**右側中央**，不併進輸入列（會誤按、會擠掉輸入框）
- 對話窗可收合看地圖；上一句還在等回覆時不能再送（**這是額度保護，不是載入旗標**）
- 聊天紀錄兩層清單、收藏方陣不分區、放大檢視圖與說明整包一起捲、點外面關閉

**推翻的**（CONTEXT 已定）：共鳴值、當日情境（含 iCulture 活動卡）、
多目標任務卡與道具格、語音輸入、語言切換。`≡` 從五項縮成三項。

**本專案特有、前身沒有的**：

- L2 就顯示靈魂立繪（前身只有相機模式才顯示）
- L3 是角色縮右下可拖曳縮放，**不是**前身那個「固定方位、要轉手機」的陀螺儀方案
- 漣漪改成**按下「呼喚靈魂」才擴散**，不隨玩家移動自己亮，圖釘也不依遠近變暗
  （2026-08-28 君和拍板：先把答案告訴玩家，呼喚這個動作就沒意義了）
- 設定裡加了**展示模式**（SDD §5.4，密語 `wanhua`）、**隱私說明**、**音樂開關與音量**

### 10.3 視覺：像素風怎麼做的

**地圖與角色像素、UI 框保持圓角** ——君和 2026-08-28 拍板的方向。

- **像素地形**：canvas 本身只有 96×170 個像素，靠 CSS 放大 ＋ `image-rendering: pixelated`。
  不是畫很多小方格——那樣既慢，縮放時還會糊掉。
- **像素邊框**：九宮格 `border-image`。`border-radius` 畫的是真圓弧，一定有反鋸齒。
  由 `tools/gen_px_frame.py` 產生（改參數重跑，**不要手改 path**，四角一定會有一角對不上）。
  三個尺度別搞混：內縮序列決定**形狀**、放大倍率決定**顆粒粗細**、slice 決定**角區大小**。
  另外 `border-image-width` 刻意大於 `border-width`——版面只佔 1px，圖片往內畫 15px，
  這樣才能用大半徑的角又不吃掉內容區。
- **字型**：Cubic 11（俐方體 11 號），SIL OFL、可商用。

### 10.4 ⚠️ 介面層踩過的坑（都很難自己想到）

1. **Svelte scoped CSS 的特異性會蓋掉全域 class。**
   元件裡寫 `.cam { border: none }` 會編譯成 `.cam.svelte-xxx`（特異性 0,2,0），
   蓋過全域的 `.ut-px-frame`（0,1,0），像素框**靜靜地消失且不報錯**。
   → 套 `.ut-px-frame` 的元件，自己的 style 裡不能寫 `border` 或 `background`。

2. **有文字又要垂直置中的地方，一定要寫死 `line-height`。**
   預設 `normal` 由字型 metrics 決定，Cubic 11 的 normal 行高偏大，
   行框比內容框高，`align-items: center` 置中的是**行框**不是字，字就浮上去。
   另外 tokens.css 有一個 `--ut-font-nudge` 微調量，套在 `.ut-txt` 上（**只給文字，不給圖示**）。

3. **點陣字型只有在原生尺寸的整數倍才銳利**（Cubic 11 是 11 / 22 / 33px）。
   本專案字級是 9～14px，所以**字是略糊的**——這是知情後的取捨，不是 bug。
   曾經試過全站改 22px，版面撐爆，已回退。
   ⚠️ 那次還疊加了「字型根本沒下載成功」，看到的是放大的黑體，差點因此否決 Cubic 11。
   **教訓：換字型與改字級是兩件事，不要一起改。**

4. **全形空格（U+3000）在 `.svelte`／`.ts` 裡會被 eslint 的
   `no-irregular-whitespace` 擋下。** 中文排版很自然會打，但它進了 JS 語法範圍就違規。
   → 用 `&#12288;` 實體或 CSS 的 `gap`。CSS 與 Markdown 裡沒事。

5. **專案原本沒設 `box-sizing: border-box`。** 已補在 tokens.css。
   沒有它，任何 `width: 100%` 加 padding 的元素都會撐出容器外。

6. **`vw` 在桌機的手機外框裡會爆。** `clamp(96px, 34vw, 150px)` 算的是瀏覽器視窗，
   不是那個 300px 的外框，立繪直接蓋滿畫面。→ 外框內的尺寸用固定 px。

7. **像素框的元件寬高不能小於 `slice × 2`**，而且**剛好等於**那個值時，
   上下角直接接合、左右沒有直邊，端點會尖成六角形。要留餘裕（目前小元件用 36px）。

8. **`.vscode/settings.json` 是 CRLF**，而 repo 走 LF、prettier 也照 LF 檢查，
   `npm run verify` 會卡在第一關 prettier，後面三關**一次都跑不到**。已修。
   ⚠️ 這也代表：`verify` 的輸出要看到 `test:guard` 那一段才算真的全過。

### 10.5 mock 層的行為（接後端時要對齊的語意）

- **距離模擬**：`WALK_PATH` 的節點就是景點座標，而且每個景點**重複一次**——
  不重複的話 50 公尺的召喚半徑換算成滑桿只有 2～3 格寬，滑過去就錯過了。
- **對話歷史**：`leave()` 先把對話存進 `history` 再清空，重進同一個靈魂會接續。
  正式版這份資料在 SDD §3.2 的 `chat_turns` 表。
- **收藏解鎖**：召喚過就解鎖那一站的相遇卡；任務卡等 P0-3、劇情卡等 P3。
- **展示模式**：開啟後 `sensed` 與 `reachable` 都直接為真，略過距離判定。
- ⚠️ **`mock/data.ts` 裡沒有任何靈魂台詞**，佔位回應全部標明是佔位。
  §3 決策 7：台詞要逐字審、不代寫、不預生成。**不要把佔位文字抄進 `content/`。**

---

## 12. 地圖層

### 12.1 兩個前提跟原始設計不一樣

**景點縮成萬華三站**（西門紅樓 → 龍山寺 → 剝皮寮）。懸了兩次的「另外兩個景點」不再是待辦。
⚠️ **`mock/` 還停在萬華三站**：`mock/data.ts` 只有三個 site id，CARDS 生成出來是 9 張。
規格是六站 15 張（`content/` 已經是六站）。**mock 跟著它自己的假資料走，切片 7 換掉它時會一起對齊。**

**地圖範圍是整個台北市**，不是萬華。這一條推翻了原本「單張 PNG」的方案（SDD §9.3 已改寫）——
也因為範圍夠大，後來加大同區三站時管線一次都沒重跑。

### 12.2 管線：四步，全在 `tools/map/`

```
step1_inspect  資料落地與覆蓋度檢查    51 秒
step2_extract  抽幾何 → npz 快取      307 秒
step3_render   光柵化成整層大圖         30 秒
step4_tiles    切成 256×256            6 秒
```

實測數字（君和的機器）：PBF `taiwan-latest` **311 MB**（不是網路上常說的 150 MB）。
建築物覆蓋率 87.7%。圖磚合計 **630 張、3.61 MB**，手機一次載視野內約 9 張 ≈ 53 KB。

**中間為什麼要插一個「抽快取」**：美術要反覆調，每調一次色就重讀 311 MB 是沒辦法工作的。
快取只有 6.6 MB，步驟 3 讀它重畫只要幾秒。索引圖（P 模式）的附帶好處是**換色不用重畫**——
比較九種調色盤時光柵化只跑了一次。

### 12.3 ⚠️ 地圖層踩過的坑（都很難自己想到）

1. **OSM 的大面積地物是 multipolygon relation，不是封閉 way。**
   v1 只讀 way，結果**陽明山整片被畫成空曠平地**——國家公園的森林是一個巨大的 relation。
   改用 `.with_areas()` 之後：陽明山變成整片森林，萬華也有 14.6% 的像素改變
   （河濱綠地、456 棟 relation 建築、建築中庭的洞）。
   ⚠️ **relation 數量少但面積極大**：green 只多 448 個形狀，點數卻從 14.9 萬翻到 29.9 萬。
   **看數量會誤判，要看點數。**
   分工：面只從 `Area` 拿、線只從 `Way` 拿，判準是 `classify()` 回的線寬（0 是面）。
   ⚠️ 挖洞一定要「全部外環畫完、再一次挖掉全部內環」——相鄰的面共用邊界時，
   後畫的外環會把前一個挖好的洞補回去。

2. **`bind:this` 綁的變數不是反應式的，除非宣告成 `$state`。**
   只在事件處理函式裡讀沒差；**但只要有 `$effect` 要讀它就會出事**——
   元素若在 `{#if}` 裡且條件是非同步才成立，`$effect` 第一次跑時元素還不存在就 return，
   之後賦值不會觸發重跑。症狀是「元素在、但初始化程式從來沒跑過」：小人是一張空白畫布。

3. **捏合縮放不要一跨門檻就跳階。** 手指還在動就連續跨門檻，縮放變成一格一格的階梯。
   正解：手指在動時用連續倍率做**預覽**，放開手才吸附到最近的一階。
   吸附用 log 比值找最近——縮放階梯是幾何級數，直接相減會偏袒間隔大的那幾階。

4. **`touch-action: none` 是地圖能用的前提。** 沒有它，捏合被瀏覽器當成整頁縮放、
   單指拖曳變成捲動頁面，地圖的 pointer 事件根本收不到完整手勢。
   同時要關掉整頁縮放（viewport 加 `user-scalable=no`）與 `overscroll-behavior: none`。
   ⚠️ 取捨：這也擋掉「放大看小字」這個無障礙手段。之後要補的是**字級設定**，
   不是把整頁縮放開回來。

5. **圖磚之間會出現 1 像素的縫**，因為瀏覽器把每張磚各自四捨五入到實體像素。
   相機位移要 `Math.round(v * dpr) / dpr` 對齊實體像素。除數是 DPR 不是 1——
   DPR 2.75 的螢幕上，CSS 的整數位移不見得落在實體像素上。

6. **地圖轉了角度之後，拖曳與長按落點都要反向換算。**
   不然轉 90 度後往上滑地圖會往旁邊跑、長按會落在偏掉的位置。
   轉動時也要按角度撐開取磚範圍 `(w·|cosθ| + h·|sinθ|)`，否則四角會露白。

7. **註解與程式不一致，遲早變成 bug。** `+page.svelte` 的註解寫「地圖仍在底下」，
   但程式是 `{:else}` 真的把 MapLayer 卸載掉。改成「蓋住」之後名實相符，
   順便省掉每次進出相機的重掛與圖磚重新解碼。

8. **下載 PBF 一定要 `curl.exe -4 -L -C -`。** 會撞到 §5.3 的 IPv6 PMTU 黑洞
   （pip 裝套件時速度掉到 35 kB/s）。`-4` 避開壞掉的 IPv6、`-C -` 斷了原指令續傳。
   Geofabrik 有 `.md5`，抓完一定要校驗——截斷的 PBF 在後續步驟會變成看不懂的錯誤。
   ⚠️ 雲端容器與掛載的 Linux VM 都連不到 geofabrik.de（egress 擋掉），只能在 Windows 上抓。

### 12.4 前端結構（接後端時不用動）

```
src/lib/shared/geo.ts          haversine（前後端共用）
src/lib/client/ui/tiles.ts     meta 載入、投影、視野 → 取磚
src/lib/client/ui/mapView.svelte.ts   相機狀態：錨點／縮放階／方位／羅盤跟隨
src/lib/client/ui/MapLayer.svelte     三層 transform：旋轉 → 平移 → 標記反向轉
```

**`haversine` 從 `server/presence/geo.ts` 搬到 `shared/`**，server 端改成 re-export，
`resolvePresence` 與那 8 個測試一行沒改。原因是 SvelteKit 會擋掉前端 import `$lib/server/*`。
**複製一份到前端是最糟的選項**——兩份會漂移，變成「前端顯示 47 公尺、後端說不在範圍內」。
`resolvePresence` 刻意留在伺服器端：前端不該有能力自己宣告「我到了」。

**相機狀態抽成 `mapView.svelte.ts` 而不是放進 `mock/session`**：
那一層是接後端時要被換掉的資料層，相機位置與縮放倍率是純畫面狀態，後端不會也不該知道。

### 12.5 展示模式：長按地圖自訂位置

SDD §5.4 的「自訂位置」以前只是文件。現在**展示模式下長按地圖 0.55 秒，玩家就跳到那裡**。
沒開展示模式而長按會跳提示，不是靜默失敗。這也順便解決了手機上摸不到走路滑桿的問題
（滑桿在手機外框下方，要捲動才看得到）。

### 12.6 地圖還沒做的

- **色調還沒調。** 現在的色盤是從前身專案 UI 色抄來的，整張圖偏同一個粉色系、明度擠在一起，
  看起來平。已經做過九種風格的比較（夜間／和風青／復古靛藍／GameBoy／薄荷／陶土／灰階／暮色／紙本），
  君和先擱置。⚠️ **結構性問題**：`街廓底` 是從建築色推導出來的（提亮 0.07 明度），
  而它覆蓋城市絕大部分面積——「整張圖都是粉的」是推導關係決定的，不是調色沒調好。
  真要調就先把它升格成自己的色票 `--ut-px-blockfill`。
  順帶一提：`--ut-px-*` 現在**只有地圖在用**（`terrain.ts` 已停用），動它不會影響 UI。
- **`terrain.ts` 的 `drawTerrain` 已停用**，留著當退路。`drawAvatar` 還在用，不要一起刪。
- **三站經緯度是地圖上讀的近似值**，等 P0-2 實地量測。剝皮寮是長條形街區，
  很可能需要 SDD §5.2 的 `extraCenters`。
- **羅盤只在安全情境有效**。本機測試要在手機 Chrome 開
  `chrome://flags/#unsafely-treat-insecure-origin-as-secure` 加入 dev 位址；上線到 https 就正常。
- **iOS 上的旋轉品質沒驗過**。`image-rendering: pixelated` 在旋轉時 Chrome 會用最近鄰，
  Safari 未確認。糊掉的話像素風會消失，要另尋辦法。

---

## 13. 後端與部署

> 後端前四支端點、從 Zeabur 搬到 GCP、實際上線。
>
> **線上網址：https://urbantales.alcloud.us**（2026-09-03 上線，21 條煙霧測試全綠）

### 13.1 先看了前身專案的後端

`C:\Users\erics\Desktop\City_Soul_AI\citysoul-backend`（FastAPI，26 個端點、11,361 行）。
君和參與過那輪開發，所以這次是「逐項確認邏輯合不合現在的設計」而不是照抄。

**邏輯相通、值得參考的三件事**：

1. **三種 token 的驗證邏輯刻意不共用**——前身在三個檔案裡把幾乎一樣的程式碼寫了三次，
   註解寫得很直白：「一旦共用，某天有人為了某個情境放寬其中一種，三種會一起被放寬，
   而 code review 只會看到一個看似無害的參數變更。」我們照做了（見 `presence.ts` 檔頭）。
2. **配額擋在最前面**——被擋下的請求不該讓下游付出任何成本。
3. **全域上限觸頂不回錯誤、安靜切保底句**——「全域上限是我們的帳單問題，不是玩家做錯事」。

**邏輯不通、不能抄的兩件事**：

- 身分：前身用 `device_id` ＋ 90 天 Bearer token（App 思維）。我們是網頁，改用 HttpOnly cookie，
  而且隱私約束下不收裝置識別碼。
- 拍照：前身 `POST /quests/{id}/landmark-photo` 會呼叫地標辨識 AI 判定照片。
  我們整個拿掉了拍照辨識，只回報「快門按了」。

**確定不抄**：TTS、推播、天氣、當日情境、長期記憶 embedding、共鳴值、每日任務重置。
全是前身「回訪機制」的枝葉，CONTEXT 已明文推翻。

### 13.2 三個拍板

| # | 決定 | 理由 |
|---|---|---|
| 1 | `/api/sites` **回經緯度，不回判定半徑** | 地圖改成真實 OSM 圖磚後，「不回座標」已經做不到（圖釘要畫對就得有經緯度，而 `mapPos` 可以反推）。承認「景點在哪」藏不住，只保密「要走多近才算到」 |
| 2 | **一定要走到 50m 內才能對話** | 前身允許 150m 感應圈就開始聊天，等於開了「不用真的到現場也能玩到 AI 對話」的路徑。只發**一張** presence token，不抄兩張制 |
| 3 | **用量計數器全放 Postgres**，不裝 Redis | 少一個服務、少一份設定、少一個會斷線的東西 |

**連帶移除 `mapPos`**：全專案沒有任何程式碼在用它，它存在的唯一理由就是決定 1 推翻的那條。
SDD 附錄 D 已改寫並附上失效理由。

### 13.3 端點與測試

| 切片 | 檔案 | 測試 |
|---|---|---|
| 訪客身分 | `hooks.server.ts`、`lib/server/auth/session.ts` | 15 條 |
| 景點清單 | `lib/server/content/sites.ts`、`routes/api/sites/+server.ts` | 11 條 |
| 到場判定 ＋ 展示模式 | `lib/server/auth/presence.ts`、`demo.ts`、`routes/api/presence/`、`routes/demo/` | 17 條 |

這三支加上原有的 8 條是 51 條；切片 4 之後 `npm test` 是 **75 條**。另外有 `npm run smoke:api`——對正在跑的 dev server
發真的 HTTP 請求，做端到端檢查（需要 dev server 在跑，所以**刻意不進 `npm run verify`**：
把需要外部服務的測試混進 verify，會讓 verify 變成時好時壞的東西，然後大家開始忽略它）。

### 13.4 ⚠️ 後端踩過的坑

**① 時鐘要統一走資料庫。**
`lastSeenAt: new Date()` 用的是**應用伺服器**的時鐘，而 `created_at` 的預設值是**資料庫**的時鐘。
兩者差了 174 毫秒，結果 `players` 出現「最後出現時間早於建立時間」這種看起來像鬧鬼的資料。
正解是 ``sql`now()` ``。之後每日額度重置、憑證效期都要比時間，混用兩個時鐘會變成
「額度沒到隔天卻重置了」這類查不出根因的 bug。**全站只認資料庫這一個時鐘。**

**② 護欄的判準要選「測得到」的，即使語意稍弱。**
`hooks.server.ts` 判斷「這個請求要不要建立玩家」時，最準的信號是 `Sec-Fetch-Mode: navigate`。
但 `Sec-` 開頭是 **forbidden header**——Node 的 fetch 會自己塞 `cors` 並覆蓋你設的值，
所以用它當判準的話，**那道閘門永遠寫不出自動化測試**，只能靠人開瀏覽器手動確認。
改用 `Accept` 含 `text/html`（文件請求一定帶，圖磚／CSS／JS 都不會命中），
`needsIdentity` 移到 `session.ts` 當純函式匯出，六條單元測試釘住。
**判準的選擇要把「這條規則將來怎麼被驗證」算進去。**

**③ 那道閘門本身是必要的，不是效能微調。**
`handle` 會攔到每一個請求，包括 630 張圖磚。玩家第一次開頁時瀏覽器同時發十幾個請求，
每一個都還沒有 cookie——不擋的話一次開頁生出十幾列 players，而且它們互相不知道對方存在。

**④ HTTP header 的值只能是 ASCII。**
用中文字串當假 token 塞進 `Cookie`，Node 在送出前就丟 `Cannot convert argument to a ByteString`。

**⑤ Drizzle Studio 不可靠。**
`npm run db:studio` 起得來，但 `local.drizzle.studio` 的分頁會卡在轉圈直到渲染程序沒回應。
要看資料就寫腳本直接查（`smoke:api` 最後會列出 players 表）。

**⑥ 測試腳本要自己收拾自己。**
`smoke:api` 每跑一次會建立 3 個玩家（沒 cookie 一個、竄改的一個、亂編的一個）。
不清的話跑十次就沒人分得出哪一列是真的玩過遊戲的人。腳本結束時會自己刪掉，
`-- --purge` 才會連先前殘留的一起清（破壞性操作要明確旗標）。

### 13.5 內容：草稿站機制

`content:check` 規則 #2 要求每站五份檔案齊全、#5 要恰好 3 題引導提問、#8 要立繪存在。
但保底台詞只有君和能寫、立繪要等 P0-1——**後端會被內容擋住**。

解法是在 `site.yaml` 加 `status: draft | playable`，**預設 draft**：

- **draft** ＝ 只有 `site.yaml`。地圖上看得到圖釘、算得出距離，但**進不去**
  （`/api/site/:id/enter` 一律拒絕），`content:check` 只驗 site.yaml。
- **playable** ＝ 五份齊全 ＋ 立繪就位，所有內容規則才對它生效。

**判準是明確宣告，不是「檔案缺不缺」**：少一個檔可能是手滑，`status: draft` 是一句聲明。
而且 `content:check` 每次都會把草稿站與各站缺件唸出來——前身的教訓是「表建好了但沒人生產內容」，
**對策不是禁止半成品存在，是不讓它安靜地存在。**

`SoulSchema.art` 因此改成可為 null，但 content:check 強制「playable 的站 art 不得為 null」。

### 13.7 從 Zeabur 搬到 GCP

**原因**：君和說 Zeabur 有資安危機。順帶關掉一個既有的面——Zeabur 的託管 PostgreSQL
掛在公開 TCP 連接埠上（`43.133.167.62:31166`），全世界都連得到，只靠密碼擋。

**做法照君和自己的兩個專案**（`Desktop\Roku_AI` 與 `Desktop\LARP_AI`，都已在 GCP 跑；
LARP 那份 `GCP_遷移設定教學.md` 是「第二次搬」的版本，寫得比 Roku 那份更細）。
新增 `deploy/` 五個檔案：`bootstrap.sh`（218 行，冪等，九步）、`urban-tales.service`、
`nginx-urban-tales.conf`、`env.example`、`README.md`。

**繼承 Roku 的兩道守門**：重建時帳號不符會擋下來（否則 systemd 的 `User=` 被改掉、
環境變數檔卻因「已存在就不覆蓋」而沒更新 → 服務讀不到金鑰，症狀是「金鑰填了卻像沒填」）；
環境變數檔重複變數名的檢查（systemd 取最後一次出現的值，空值會蓋掉填好的值）。

**自己加的一道**：建置前先開 2GB swap。e2-small 只有 2GB，`vite build` 可能被 OOM killer 砍掉，
症狀是「`npm run build` 沒有錯誤訊息就中斷了」。

**環境變數改名 `AIHUB_*` → `AI_*`。** 供應商名字不該進變數名——第一版綁死 Zeabur，
搬家時改的全是名字、邏輯一行沒變。

#### ★★★ 這次搬家最重要的一個發現 ★★★

君和在 Roku 的 VM 上實測過（2026-09-01，真實 system prompt，中位數）：

| 模型 | 預設 thinking | 延遲中位數 |
|---|---|---|
| `gemini-3.7-flash` | medium | **8.05 秒**（4.71–13.91） |
| `gemini-3.5-flash-lite` | minimal | **0.74 秒**（0.58–0.98） |

**SDD §6.5 的逾時是 8 秒，而 medium 思考的中位數就是 8.05 秒。**
拿它當主要模型，一半以上的對話會逾時走保底台詞——而且開發時很可能剛好都落在 8 秒內，
上線後才發現。所以 `AI_MODEL_PRIMARY` 必須是 flash-lite 那一級。
**這不是省錢考量，是能不能用的問題。** 而且我們比 LINE bot 更禁不起慢：玩家站在廟口等。

### 13.8 正式站

**https://urbantales.alcloud.us** —— 憑證到 2026-12-02，certbot 自動續約。

| 項目 | 值 |
|---|---|
| GCP 專案 | `larp-507213`（與謎案迴聲同專案，us-central1-a） |
| VM | `urbantales`／e2-small／Ubuntu 24.04／平衡永久磁碟 20GB |
| 外部 IP | `34.41.151.184`（靜態，`urbantales2`） |
| 登入 | 帳號 `al06120001`，金鑰 `C:\Users\erics\.ssh\gcp_larp` |
| 環境變數 | `/etc/urban-tales/urban-tales.env`（640 root:al06120001） |

**為什麼建在 larp 專案裡**：GCP 對「一個帳單帳戶能綁幾個專案」有配額，君和的已經滿了。
同專案只共用帳單／SSH 金鑰／VPC，**VM、磁碟、IP、nginx、憑證、服務全都各自獨立**。

日常部署與煙霧測試的指令見 `deploy/README.md`。

### 13.9 ⚠️ 上線那天踩到的三個坑

**① 關機順序：`SHUTDOWN_TIMEOUT` 必須小於 `TimeoutStopSec`。錯了兩次才對。**

adapter-node 收到 SIGTERM 的流程是：**先等現有連線收乾淨（預設 30 秒），
等完才發出 `sveltekit:shutdown` 事件**——而關閉資料庫連線池的程式碼掛在那個事件上。

| 嘗試 | 結果 |
|---|---|
| 原始設定 | 等滿 **90 秒**（systemd 預設）被 SIGKILL。根因：postgres-js 連線池掛在事件迴圈上，`server.close()` 之後行程仍不退出 |
| 加 `TimeoutStopSec=20` | 等滿 **20 秒**被砍。**因為 20 < 30，systemd 在 adapter-node 還在等連線時就砍了，關連線池的程式碼根本輪不到執行** |
| 再加 `SHUTDOWN_TIMEOUT=5` | **0.06 秒**，`Deactivated successfully` |

`src/lib/server/db/index.ts` 的 `sveltekit:shutdown` 處理器裡留了兩行 log
（`[shutdown] SIGTERM —— 關閉資料庫連線池` / `[shutdown] 連線池已關閉`）。
**關機出問題時那是唯一能分辨「事件沒發出」與「連線池關不掉」的線索，不要刪。**

**② 掃描器會建立玩家。上線十一分鐘就多了 18 列。**

公開 IP 每天都被戳 `/about`、`/login.action`、`/.env`、`/.git/config`……
那些路徑不存在、回 404，但只要帶著 `Accept: text/html` 就會通過 `needsIdentity`
拿到一張 cookie 與一列玩家。

解法：`hooks.server.ts` 用 `event.route.id === null` 擋掉——比對不到任何路由
＝這個請求最後會是 404，那種請求不該在資料庫留下任何東西。
**刻意不寫進 `needsIdentity`**：那是純函式，看不到路由表，硬塞就得複製一份路由清單給它維護。

**③ 同一條規則在兩個環境的預期可能相反。**

`smoke:api` 有兩條檢查在正式站會失敗，但**伺服器的行為是對的**：

- `x-ut-identity` 是 dev 專用的除錯 header，正式環境不發是設計（回應 header 是公開資訊）
- `Secure` 在 https 下**必須**有、在 http 下**必須**沒有（程式碼是 `secure: !dev` 一行）

腳本現在用 `isProd` 讓斷言跟著協定翻轉。**寫測試時要問「這條在正式環境的預期一樣嗎」。**

## 14. 後端切片 4：進入景點

> `POST /api/site/:id/enter`：草稿站拒絕、首次進入發相遇卡（冪等）。
> 另外順手抓到一個躺了六天的型別錯誤，以及它躺得住的原因（§14.5，比錯誤本身重要）。

### 14.1 本機開發環境怎麼接

搬到 GCP 之後開發環境壞了兩件事，都修好了：

- 本機 `.env` 的 `AIHUB_*` 全部改名 `AI_*`，模型改成 Gemini（primary 是 `gemini-3.5-flash-lite`）
- `DATABASE_URL` 改成走 SSH 通道 `127.0.0.1:55432`
- 原檔備份在 `.env.bak`（`.gitignore` 的 `.env.*` 蓋得到，不會進版控）

**兩個祕密（DB 密碼、`AI_API_KEY`）直接從 VM 抄。** `/etc/urban-tales/urban-tales.env`
是 `640 root:al06120001`，君和的帳號在群組裡，**不用 sudo**：

```
ssh -i C:\Users\erics\.ssh\gcp_larp al06120001@34.41.151.184 "grep -E '^(AI_API_KEY|DATABASE_URL)=' /etc/urban-tales/urban-tales.env"
```

⚠️ **`SESSION_SECRET` 刻意不從 VM 抄。** 在場憑證與展示模式憑證都是從它推導的，
本機若用正式那把，開發機就能簽出**正式站認得的憑證**。兩邊各一把，不要為了方便共用。

⚠️ **`AI_API_KEY` 的長度會騙人。** Gemini 新格式是 `AQ.Ab8…` 53 字元，
跟舊的 Zeabur AI Hub token 一樣長。只看長度分不出來，要看開頭。

本機 `DEMO_PASSPHRASE` 填了 `urban-tales-dev`（原本是空的＝展示模式整個關掉）。
沒有它就沒辦法在不走到西門町的情況下測進入景點。**正式站那把不同，也不該相同。**

### 14.2 做完的切片 4

| 檔案 | 做什麼 |
|---|---|
| `lib/server/content/souls.ts` | 靈魂載入器 ＋ `toPublicSoul()` 白名單 |
| `lib/server/content/cards.ts` | 卡片載入器 ＋ `getEncounterCard()` |
| `lib/server/site/enter.ts` | ★ `decideEnter()` 純函式，所有拒絕判準都在這裡 |
| `lib/server/progress/award.ts` | `awardCard()` / `markFirstMet()`，兩支都冪等 |
| `routes/api/site/[id]/enter/+server.ts` | 路由，很薄 |
| `scripts/content-check.ts` | 新增 #7b |
| `scripts/smoke-api.ts` | 新增四條端到端檢查 |

`npm test` 現在是 **75 條**（原 51 ＋ 新 24）。

### 14.3 四個拍板

| # | 決定 | 理由 |
|---|---|---|
| 1 | 在場憑證走 **`X-Presence-Token` header** | 不用 `Authorization`：那在所有人的直覺裡代表「你是誰」，而在場憑證是「你剛才站在哪」。混用會讓後來讀程式碼的人以為兩者可以互換——那正是 `presence.ts` 檔頭花一整段避免的事。不放 body：chat 與 photo-task 要帶同一張，放 body 就是三份會漂移的 schema |
| 2 | **enter 只回卡片與立繪**，不回開場白與引導提問 | 那兩樣來自 `prompts.yaml`，三站都還沒有。挑題邏輯（tier／triggerTopics 去重）本來就是切片 5 的事，現在寫了也驗證不了 |
| 3 | **發卡的 id 用查的，不用拼的** | `encounter-<siteId>` 這種約定是一份沒寫下來的規格。哪天 cards.yaml 命名不同，程式碼會照樣寫進一個不存在的 card_id，**而且不會有任何錯誤**——玩家圖鑑永遠缺一張，資料表裡卻有一列。查表則是查不到就當場知道 |
| 4 | **不建測試用假景點** | 假景點會混進 `/api/sites`，也就會出現在地圖上，得再想辦法把它從正式建置排除——多一套排除機制就多一個會失效的地方。成功路徑由 `enter.spec.ts` 的純函式覆蓋 |

### 14.4 ⚠️ 切片 4 的三個判斷（都寫進註解了）

**① `persona` 絕不外流。跟 `radiusM` 同一類錯。**
persona 整段會逐字進 system prompt（SDD §6.2 的 [1]），洩漏等於把護欄怎麼寫、
角色被禁止說什麼一起交出去——越獄從「試探」變成「照著繞」。
而且它是「錯了畫面完全正常」的那種錯：多回一個欄位，前端不會壞、UI 不會變、沒有人會發現。
`souls.spec.ts` 那條測試不只比對欄位名，還**逐字比對內容有沒有漏出去**。

**② 判斷順序本身是規格。**
`identity → 有沒有這站 → 草稿 → 憑證`。由「最不需要祕密的判準」往「最需要的」排，
每一層洩漏的資訊都不超過 `/api/sites` 已經公開的東西（`status` 本來就在回應裡）。
反過來排不會比較安全，只會讓每個戳草稿站的請求都白驗一次 JWT。
**兩條測試釘住這個順序**（純函式一條、smoke 一條），有人挪動就會失敗。

**③ `awardCard` 無條件呼叫，不是「firstMet 才發」。**
`markFirstMet` 與 `awardCard` 之間若斷線，玩家會停在「已記錄第一次相遇、卡卻沒發出去」
而且**再也回不去**——下一次 `markFirstMet` 會回 false。無條件呼叫讓下一次請求自己把卡補上。
**能自癒的順序，勝過一個交易。**

### 14.5 ★★★ `verify` 的判準跟 CI 對不上，六天沒人發現 ★★★

`npm run check` 報了一個 `MapLayer.svelte:134 'meta' is possibly 'null'`。
它是**地圖那一輪（2026-08-28）引入的**，躺了六天。

**為什麼躺得住**：`ci.yml` 其實**有**跑 `npm run check`，但 `npm run verify` **沒有**。
而 HANDOFF §4 寫著「`verify` 是提交前該跑的那一個指令」。於是：

> **「verify 過了」不等於「CI 會過」。六天就掉在這個縫裡。**

**已修**：`verify` 現在是 `content:check → lint → check → test → test:guard`。
代價是它從 5 秒變成 25–30 秒（svelte-check 要啟 Svelte 語言伺服器），值得。
`build` 沒加——它要 `DATABASE_URL` 且要一分多鐘，加了就真的會讓人不想跑。

**錯誤本身的成因值得記**：TypeScript 的**縮小推論不會穿進閉包**。

```ts
if (!meta) return;                       // 這裡縮小成功
const mpp = (s) => meta.levels[...]      // ❌ meta 是 $state（可變），
                                         //    TS 不敢假設閉包執行時它還是非 null
```

同一個檔案的 `screenToLonLat` 一樣先擋 null 卻沒報錯，因為它**直接**用 `meta`、沒有閉包。
修法是先 `const m = meta` 把值釘住。執行期原本就是安全的，但型別要跟事實一致。

### 14.7 ⚠️ verify 抓不到的四種錯

跑 verify 之前先做了一次靜態核對，抓到一個**會讓 smoke 直接 500 的錯**。
記在這裡是因為它屬於「verify 抓不到」的那一類，跟 §14.5 是同一個形狀。

**① ★★★ `+server.ts` 只能匯出 HTTP 方法。★★★**

`PRESENCE_HEADER` 原本定義並匯出在 `routes/api/site/[id]/enter/+server.ts` 裡。
SvelteKit 的 `validate_server_exports`（`node_modules/@sveltejs/kit/src/utils/exports.js`）
只放行 `GET / POST / PATCH / PUT / DELETE / OPTIONS / HEAD / fallback /
prerender / trailingSlash / config / entries`，以及**底線開頭**的名字。
其餘任何具名匯出會丟 `Invalid export 'X' in +server.ts`。

⚠️ **危險的是它的發生時機**：那是模組載入時的執行期檢查。
`npm run verify` 全綠、`npm run check` 也全綠，要到 dev server 收到第一個請求才炸。

已修：常數搬到 `lib/server/auth/presence.ts`，`chat` 與 `photo-task` 之後共用同一個。
全專案四支 `+server.ts` 都掃過，其餘乾淨。
**要在路由檔旁邊放常數的話，名字加底線前綴，或者搬到 `$lib`。**

**② smoke 腳本自己又寫了一次 header 名字。**
改成從 `presence.ts` import。兩份字串會漂移，而漂移的那天測試會「通過」但實際是錯的
——比沒有測試更糟。

**③ `content:check #7b` 在沒有 playable 站時完全不出聲。**
補了「尚未啟用」的 note。#7 未啟用會印、#7b 不印，那違反本專案
「不讓它安靜地存在」的原則：**「這條規則現在沒在檢查」與「這條規則檢查過了」
必須看得出差別。**

**④ drizzle 把底層錯誤吞掉了 —— 補了 `npm run db:ping`。**

SSH 通道斷掉時（**電腦睡一次就會斷，而 `-N` 的終端機不噴任何訊息**），
瀏覽器上看到的是：

```
Error: Failed query: insert into "players" (...) values (...)
    at Object.handle (src/hooks.server.ts:125)
```

——完全看不出是「連不上」「密碼錯」還是「表不存在」。三種原因、三種修法，
而錯誤訊息一視同仁。`scripts/db-ping.ts` 直接用 postgres-js 連，把原始錯誤碼
印出來並附上**怎麼修**（ECONNREFUSED / ETIMEDOUT / 28P01 / 3D000 / 42P01），
然後列出資料表與筆數、比對 schema.ts 的七張表在不在。

⚠️ 它不進 `verify`（需要外部服務，同 smoke:api 的理由）。
⚠️ 連線字串印出來前會把密碼遮掉。

> **更廣的教訓**：ORM 的錯誤訊息會為了統一格式而丟掉診斷資訊。
> 只要某個外部依賴「斷掉時的症狀」是一句看不出原因的話，就值得為它寫一支
> 印得出原因的腳本——**猜測換成一行指令，那支腳本會被用很多次。**

**查過沒問題的三項**（記下來，免得下次重查）：

- `needsIdentity` 第一行就是 `if (pathname.startsWith('/api/')) return true`，
  新端點不會被身分閘門誤判成子資源。
- drizzle 0.45.2 的 `setWhere` 確實會產生 `where` 子句，且
  `${playerSiteState.firstMetAt}` 展開成 `"player_site_state"."first_met_at"`
  （帶表名，正確指向既有列而不是 `excluded`）。`markFirstMet` 的冪等判斷成立。
- migration `0000` 已含 `player_site_state` 與 `player_cards`，複合主鍵與 cascade 外鍵都在。

### 14.8 ⚠️ 切片 7 不只是「換資料來源」

比對前端 mock 層與真後端，有**四個**對不上的地方。現在不會出事（前端全是假資料），
但切片 7 會全部撞上。**先記下來，免得那時候以為只要換 fetch。**

| # | mock 現況 | 真後端 | 影響 |
|---|---|---|---|
| 1 | `mock/data.ts` 的 SITES 有 **`summonM`**，`session.svelte.ts:171` 用它在**前端**算 `reachable` | `radiusM` **永遠不進 API 回應**（附錄 D） | 切片 7 要**整段拿掉前端的到場判定**，改由 `/api/presence` 回答。這是刪邏輯，不是換資料來源 |
| 2 | ★ `WinSettings.svelte:14` 有 **`const DEMO_PHRASE = 'wanhua'`**，比對通過就直接設 `session.demoMode = true`，**完全沒有碰伺服器** | `GET /demo?key=` 比對 `env.DEMO_PASSPHRASE`，發簽章 cookie `ut_demo`（2 小時） | 見下方 ★★ |
| 3 | `CardKind` 是 `'encounter' \| 'task' \| 'arc'` | `content/schema.ts` 是 `'encounter' \| 'task' \| 'story'` | 名字漂移。接上去會靜默對不到，圖鑑少一整類 |
| 4 | 卡片 id 是 `encounter-${s.id}` 拼出來的 | 由伺服器查 `cards.yaml` 給 | 正是 §14.3 拍板 #3 否決的那個約定。前端要改成用伺服器回的 id |

★ **第 1 項與第 2 項是同一種病**：**前端有能力自己宣告一件該由伺服器決定的事。**
第 1 項是「我到了」，第 2 項是「我是展示模式」。兩個都與核心設計第 1 條「硬到場」相衝，
現在無害只因為它們連的是假資料。接上真後端那一刻，**那兩段程式碼必須消失，
不是改成讀 API 的值。**

★★ **第 2 項還多一層安全性後果，切片 7 不做不行：**

`DEMO_PHRASE` 寫在前端 = 任何人打開 devtools 或直接看 JS bundle 就讀得到，
**等於密語根本不存在**。`demo.ts` 的檔頭已經先警告過這件事：
「前端那個開關不能是唯一的判準……真正決定的是這張伺服器簽的 cookie。」

**接起來之後的流程**（玩家全程看不到網址，303 之後它就離開網址列了）：

```
設定裡輸入密語
  → 前端導向 /demo?key=<玩家輸入的字>      ← 前端不知道、也不該知道正確答案
  → 伺服器常數時間比對 env.DEMO_PASSPHRASE
  → 對了發 ut_demo cookie（2 小時）
  → 303 導回 /
```

⚠️ **同時要刪掉 `session.demoMode` 這個前端布林值。** 展示模式的唯一真相是那張 cookie；
留一個前端變數在旁邊，遲早有人拿它當判準。

⚠️ **換密語不會把已經發出去的通行證收回來。** `ut_demo` 是用 `SESSION_SECRET`
推導的金鑰簽的，不是用密語簽的。要立刻全部踢掉只能換 `SESSION_SECRET`，
而那會連在場憑證一起作廢。實務上等兩小時到期即可——但「換密語＝立刻鎖門」這個直覺是錯的。

### 14.9 驗收佐證：草稿站的拒絕路徑一個位元組都沒寫進資料庫

切片 4 驗收時 `npm run verify` 0 errors／75 條測試、`npm run smoke:api` 24 條全綠。

★ 真正值得記的是這個：驗收後 `player_site_state` 與 `player_cards` **都是 0 列**。
那不是「沒測到」，是**草稿站的拒絕路徑真的一個位元組都沒寫進資料庫**——
判斷順序排在憑證之前那件事，效果在這裡看得見。

⚠️ **正式庫的 `players` 有 21 列殘骸**（上線那天掃描器建的 18 列 ＋ 君和開頁測試的幾列），
目前**沒有任何真玩家**。要清就趁現在：`npm run smoke:api -- --purge`。
一旦有真玩家就不能再跑那個旗標了，那時要另開 `urban_tales_dev`（見 §0）。

---

## 15. 內容層的形狀

> 內容層的兩個關鍵形狀：素材庫是**一段 `facts` ＋ 幾條 `legends`**，
> 引導提問是**恰好 3 題、寫死**。這一節說明為什麼是這個形狀——
> 兩者都推翻過更複雜的設計，理由值得記住。

### 15.1 原料倉庫：前身素材盤點

先把前身 `City_Soul_AI` 十個景點的**對話文本與史實**整理成一份逐字盤點：

**`City_Soul_AI_對話素材盤點.md`**（1,716 行，專案根目錄）

⚠️ **它不是內容層**：不進 `content/`、不受 `content:check` 檢查、程式不讀。
是寫內容時的原料倉庫。

⚠️ **`C:\Users\erics\Desktop\City_Soul_AI` 不在預設連接的資料夾裡。**
每個新對話要重新請求存取權才讀得到。

**最重要的發現**：前身的「已具名審閱」範圍比想像的窄——審的是**人格與少數幾句**，
不是題庫。前身的引導提問是**即時生成**的，`guided_question_fallback` 那三句只是
「生不出來時的退路」。**所以每站 12 題的舊要求，前身一題都沒有。**

### 15.2 兩個改版

| # | 原本 | 改成 | 誰推翻的 |
|---|---|---|---|
| 1 | `materials.yaml` 是 `items` 陣列，每條標 `kind`（已知史實／民間傳說／角色想像）＋ `topic` ＋ `source` | **一段 `facts` ＋ `sources` 陣列 ＋ 幾條 `legends`** | 企劃書 §4.2 第 5 條的三分改兩分 |
| 2 | `prompts.yaml` **≥ 12 題**，分 `tier`（opening／followup／safety）、`triggerTopics`、`topic`，由 `pickPrompts()` 依對話進度挑 | **恰好 3 題，寫死，不挑選** | SDD T5 ＋ 企劃書 §5.3 |

#### 為什麼素材庫改成「一段 ＋ 幾條」

君和寫內容時是「一站寫一段」，不是「一站列十三條」。條目制逼他把一段連貫的敘述
切碎成沒有上下文的句子，**而模型拿到碎片，出來的話也像碎片。**

⚠️ **但不能乾脆合成一整段。** `legends` 必須分開，因為那個區分決定靈魂
**用什麼語氣講一句話**：

- `facts` → 可以講得斬釘截鐵（「一九〇八年落成，近藤十郎設計」）
- `legends` → 必須留活口（「有一種說法是⋯⋯，也有人說⋯⋯」）

混成一段的話，「八卦祈福、十字架鎮邪」跟文化局的古蹟公告在模型眼裡長得一樣，
它會用同一種語氣講出來。**剝皮寮的「剝皮」由來是必踩的那一題。**

★ **「角色想像」那一類移出素材庫了**——它不是素材，是**權限**。
「這個靈魂可以描述哪些沒被記錄下來的東西」定義在 `soul.yaml` 的 `persona.knows`。
三者並列在同一個欄位裡，實務上會發生一件事：寫的人為了讓靈魂能講某句話，
就把它標成「角色想像」塞進素材庫——**那等於在素材庫裡開一個不必查證的抽屜。**

#### 為什麼引導提問從 12 題縮成 3 題

原本 12 題的理由是企劃書 §5.3 的「重複感是玩家判定『這是機器人』的主因」。
**那條理由成立的前提是玩家會反覆看到這些提問。** 但本專案：

- **沒有回訪機制**（CONTEXT 明文推翻），一趟就拿完一個景點的卡；
- 引導提問是**卡住時的起手式**，不是主要互動——按下去之後就進自由對話了；
- 只做六站，一個玩家整趟總共看到 18 題——而且是六個不同靈魂各三題，不是同一批。

在這個形狀下，12 題的池子換來的不是「不重複」，而是
**九成的題目玩家永遠看不到，卻一樣要逐字審**。成本全付，好處沒拿到。

⚠️ **`.length(3)` 而不是 `.min(3)`**：L2 的版面是固定三個按鈕。允許多寫的話，
前端就得決定「多出來的怎麼辦」（隨機抽三個？滾動？）——**那就是把剛拿掉的
挑題邏輯又請回來。** 恰好三題讓「多寫了」在建置時被擋下來，不是上線才發現版面破了。

### 15.3 ★ 規則編號不重用

`content:check` 的舊 #4 是「prompts 的 topic 要在 materials 找得到」。`topic` 欄位
移除之後那條失去依據，新的 #4 是另一條規則（legends 空的要唸出來）。
**編號沒有回收再利用**——重用編號會讓「#4 是什麼」在不同文件裡有兩個答案。

同一個原則也用在這份 HANDOFF 的章節編號上（見檔頭）。

---

## 16. 內容：六站

> **景點範圍是六站**（原本只做萬華三站），
> 六站各四份 YAML 就位（共 24 份，缺 `fallbacks.yaml`），`npm run verify` 全綠。

### 16.1 景點範圍

**取代 2026-08-28 的「只做萬華三站」。** 六站：

| 行政區 | 站 | site id | 角色 | 劇情層 |
|---|---|---|---|---|
| 萬華 | 西門紅樓 | `ximen-red-house` | 女 23，開朗健談、自嘲話多 | ✅ 1 |
| 萬華 | 艋舺龍山寺 | `longshan-temple` | 男 65，爽朗好客、一眼看出來意 | ✅ 2 |
| 萬華 | 剝皮寮歷史街區 | `bopiliao` | 女 63，溫柔、記性選擇性地不好 | ✅ 3 |
| 大同 | 臺北當代藝術館 | `moca-taipei` | 女 32，文靜靦腆、學院派 | — |
| 大同 | 臺灣新文化運動紀念館 | `new-culture-movement` | 男 42，嚴謹寡言、偶爾不耐煩 | — |
| 大同 | 臺北霞海城隍廟 | `xiahai-temple` | 女 36，御姐、愛捉弄人 | — |

**劇情層仍然只有萬華三站**，大同區三站是純基礎層。

✅ **地圖沒有付出代價**——2026-08-28 把圖磚範圍從萬華改成整個台北市（§12.1），
大同區三站本來就蓋得住。當時那個「範圍卻是整個台北市」的決定，在這一刻回收了。
要補的只有三組真實座標與 `radiusM`（P0-2）。

### 16.2 ★★★ 前身盤點文件的史實表有五個硬傷，不要照抄

`City_Soul_AI_對話素材盤點.md` 是逐字轉錄，**轉錄不等於正確**。查證後修掉：

| # | 前身寫的 | 正確的 | 為什麼要緊 |
|---|---|---|---|
| 1 | 龍山寺「自**安溪**分靈」 | 福建晉江**安海**龍山寺 | 安溪人在艋舺信清水祖師，跟三邑人在頂下郊拚是**對立陣營**。寫錯會把族群關係整個講反 |
| 2 | 龍山寺 1919 年匠師「**辜婦蠢**」 | **辜顯榮**，而且他是**董事長**不是匠師；匠師是王益順 | 名字是訛寫，角色定位也錯 |
| 3 | 剝皮寮「1941–**1945** 禁建」 | 禁建延續到 **2003 年前後** | 「四年」與「六十年」是完全不同的故事 |
| 4 | 西門紅樓「**1945** 改滬園劇場」 | **1949**，上海商人陳惠文承租 | — |
| 5 | 西門紅樓「1963 改紅樓戲院」 | 1951 紅樓書場 → 1956 紅樓劇場 → **1963** 紅樓戲院 | 中間漏了兩次改名 |

另有一項**性質誤判**：西門紅樓「八卦祈福、十字架鎮邪」在前身與官方觀光文宣裡
被當設計理念講，但沒有文獻證據，而且被研究者具體反駁（興建前墳墓已遷葬、
大門開向東北的鬼門、動工日是農曆月破日）。**它進 `legends`，不進 `facts`。**

> **更廣的教訓**：「已具名審閱」的範圍比想像的窄（§15.1 已經記過一次）。
> 前身審的是**人格**，不是史實表。拿前身的資料當原料可以，當定稿不行。

### 16.3 人格怎麼寫才立體：三個零件

一串形容詞（開朗、溫柔、嚴謹）給出的是**標籤**，模型拿到標籤只會把每句話塗上
同一種顏色。六站的 `persona` 都按這三個零件寫，**新增景點照這個做**：

1. **一個矛盾**——角色想要的跟它的處境相衝。給動機，不只給語氣。
   （龍山寺天天有人來求東西，而它什麼都給不了；當代藝術館是第一座當代藝術館，
   卻沒有自己的典藏；新文化運動紀念館紀念言論自由，而關過蔣渭水的是上一棟）
2. **一件在意到不成比例的事**——它**主動岔題**的燃料。不岔題的角色一聽就是機器。
3. **一道裂縫**——一個話題會讓常態語氣掉下來一瞬間。玩家記得的是這個。

⚠️ 三個零件都要**能從 `materials.yaml` 長出來**。憑空給創傷是同人設定，不是城市靈魂。
⚠️ 裂縫不等於崩潰——它是一個地方，不是需要被安慰的人。

設計說明留在 `對話內容規劃_v0.1.md`，那份**不是內容層**，是驗收基準。
★ 規劃稿裡「引導對話選項」的**回答不進任何 YAML**——提問按下去等同玩家自己打字
送出，走完整 `speak()` 流程。那些答案是拿來對照模型講得對不對的，不是預寫台詞。

### 16.4 ⚠️ 寫內容踩過的坑

**① ★★★ `content:check #9` 掃的是子字串，寫在否定句裡一樣會被擋。**

`scripts/content-check.ts` 對 `soul.yaml` 的 `persona` 做
`JSON.stringify(...).includes(phrase)`，比對 `schema.ts` 的
`FORBIDDEN_PERSONA_PHRASES` 十個字串。**它不看語意**，所以這些全部 fail：

- `我不替任何人、任何神明說話。` ← 含「神明說」，而這句意思完全正確
- `絕不說「菩薩告訴我」這種話。` ← 引用被禁原句當反例
- 寫在 `|` block scalar 裡的 ⚠️ 提醒也算——**block scalar 的內容是值，不是註解**

第三點是我自己踩的：第一次修的時候把「避開『神明說』三個字」這句提醒寫進
`knows` 裡，結果又被掃到一次。

正解：要限制自己不替神明說話就寫「不替任何一位神祇**傳話**」；
要提醒後人就寫成 **YAML 註解**（`#` 開頭不進值）。兩間廟的 `soul.yaml` 檔頭都留了說明。

> 這是刻意的取捨，不是 bug。靜態檢查要能機械執行就不可能懂否定句，
> 而「寧可誤擋也不要漏放」對安全規則是對的方向。

**② `persona.taboos` 不要複誦全域護欄。**

`taboos` 的定位是「**全域護欄之外的追加**」。前身三站有一批條款跟
`content/guardrails.yaml` 一字不差——「不給具體的醫療、法律、投資建議」＝第 6 條，
龍山寺還有四條＝第 3 條。全部刪掉。護欄實測已佔 **1,139 tokens**（§6 P0-5），
每站複誦一次就多付一次。

**③ ★★ 測試把「三站」寫死了，擴到六站時三條紅掉。**

`sites.spec.ts` 與 `souls.spec.ts` 的站名清單是硬編碼的，`allSiteGeo()` 還
`toHaveLength(3)`。**這不是測試寫壞了，是絆線正常運作**——它擋下了一次
「內容加了、周邊沒跟上」的提交。

所以修法是**把清單抽成 `SITE_IDS` 常數並擴到六站，不是改成掃 `content/sites/`**。
掃出來的版本永遠會通過，那等於沒有測試。常數上方留了註解，說明要改站數就得順便
回答三個問題：地圖範圍夠不夠、P0-2 要多量幾個點、`content-check.ts` 的
`EXPECTED_SITE_COUNT` 與 `EXPECTED_CARDS` 要不要跟著動。

### 16.5 ⚠️ 霞海城隍廟是六站風險最高的

理由不是人格，是題目：全臺最知名月老廟，「我什麼時候會遇到對象」
**會是最高頻問題，不是邊緣情況**，而護欄第 3 條禁止預測吉凶與代神明應許。

三個對策（都已落實）：

1. **`prompts.yaml` 三題全部避開姻緣**——引導提問等於幫玩家開口，
   我們不主動把話題開到護欄邊上。那個檔的檔頭寫了說明，之後有人想加一題月老的，
   先讀那一段。
2. `isNot` 明確排除「月老本人」與「代替月老賜籤／牽紅線」。
3. **`fallbacks.yaml` 的 `refusal` 要優先寫**，排在其他站前面。

★ 「捉弄人」的人格是最好的載體：婉拒可以俏皮而不失禮，
比正經八百說「我不能回答這個問題」自然得多。捉弄的對象是問題，不是人。

**龍山寺的「一眼看出來意」同理**：必須歸因於「看人看了幾百年」（護欄第 2 條要的
正是這個），而且猜到之後**不評論、不預測結果**。

### 16.7 保底台詞的四條判準

六站各 14 句（五種情境 3／3／3／2／3，共 84 句）在 `content/sites/<id>/fallbacks.yaml`。
新增景點時照這四條做：

**① 失效的理由用場域裡的聲音或角色自己的狀態。** 玩家不該從台詞裡推得出來
是 AI 掛了還是額度用完了。六站各有各的藉口（市集人聲／誦經／巷子回音／上課鐘／
街口車聲／卸貨），不要六站都寫「太吵」。

**② ★★★ `aiUnavailable` 至少要有一句不把球丟回給玩家。**
這一組同時覆蓋 **L4 全域成本上限**觸頂（SDD §10.1），而 L4 會撐到隔日 UTC+8 00:00。
第一版十八句**全部**是「我沒聽清，再說一次」——玩家重問兩輪就會斷定「程式壞了」，
而 SDD 要的效果是「這位靈魂今天話少一點」。所以每站第三句都**收掉回合**。

**③ 不能假設玩家人在現場。** 實地模式與展示模式看的是同一組字，而展示模式的玩家
往往連台北都不在——而且他們是**在評估這個作品的人**（密語放在作品集頁面上）。
⚠️ **人格卡允許的現場關心不能照抄進保底台詞**：人格卡走 AI、拿得到 `presenceMode`
可以自己調；保底台詞是兩種模式共用的同一組死字。
（代價：剝皮寮 `voice` 裡的招牌「你熱不熱」只能改成「你累不累」。
技術上可以讓 `fallbacks.yaml` 分 field／demo 兩套，但 14 句裡只有兩三句吃得到這個差別，
不值得把內容量翻倍。）

**④ 拒絕要給替代，而且替代要兌現得了。** 只能指向 `materials.yaml` 真的有的東西。
承諾「那個有紀錄」而素材庫沒有，比直接拒絕更糟。

⚠️ **六站的句子不共用。** 第一版照同一個模板填，結果六個靈魂聽起來像同一個人——
`offTopic` 第一句六站只換一個名詞、`quotaReached` 六站都收在「明天再來，我還在」、
北署與紅樓的拒絕句只差一個字。重寫後跨站相似度全部低於 0.55。
